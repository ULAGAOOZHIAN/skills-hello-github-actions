name: Simple CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run script - 24ds1000121@ds.study.iitm.ac.in
        run: echo "Hello, world!"
